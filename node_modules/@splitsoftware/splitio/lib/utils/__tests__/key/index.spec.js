"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs3/helpers/interopRequireDefault");

var _typeof2 = _interopRequireDefault(require("@babel/runtime-corejs3/helpers/typeof"));

var _tapeCatch = _interopRequireDefault(require("tape-catch"));

var _parser = _interopRequireDefault(require("../../key/parser"));

var _factory = require("../../key/factory");

/**
Copyright 2016 Split Software

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
**/
(0, _tapeCatch.default)('KEY PARSER / if a string is passed a param should return a object', function (assert) {
  var key = 'some key';
  var keyParsed = (0, _parser.default)(key);
  assert.equal((0, _typeof2.default)(keyParsed), 'object', 'key parsed should be a object');
  assert.true(keyParsed.matchingKey && keyParsed.bucketingKey, 'key parsed should has two properties');
  assert.equal(keyParsed.matchingKey, key, 'matching key should be equal to key');
  assert.equal(keyParsed.bucketingKey, key, 'bucketing key should be equal to key');
  assert.end();
});
(0, _tapeCatch.default)('KEY PARSER / should return the keys configurations', function (assert) {
  var matchingKey = 'some key';
  var bucketingKey = '100%:on';
  var keyParsed = (0, _parser.default)({
    matchingKey: matchingKey,
    bucketingKey: bucketingKey
  });
  assert.equal((0, _typeof2.default)(keyParsed), 'object', 'key parsed should be a object');
  assert.true(keyParsed.matchingKey && keyParsed.bucketingKey, 'key parsed should has two properties');
  assert.equal(keyParsed.matchingKey, matchingKey, 'matching key should be equal to matchingKey');
  assert.equal(keyParsed.bucketingKey, bucketingKey, 'matching key should be equal to bucketingKey');
  assert.end();
});
(0, _tapeCatch.default)('FACTORY KEY PARSER / if a string is passed as a param it should return a string', function (assert) {
  var key = 'some key';
  var keyParsed = (0, _factory.matching)(key);
  assert.equal((0, _typeof2.default)(keyParsed), 'string', 'key parsed should be a string');
  assert.equal(keyParsed, key, 'key parsed should be equal to key');
  assert.end();
});
(0, _tapeCatch.default)('FACTORY KEY PARSER / if a object is passed as a param it should return a string', function (assert) {
  var key = {
    matchingKey: 'some key',
    bucketingKey: 'another key'
  };
  var keyParsed = (0, _factory.matching)(key);
  assert.equal((0, _typeof2.default)(keyParsed), 'string', 'key parsed should be a string');
  assert.equal(keyParsed, key.matchingKey, 'key parsed should be equal to key');
  assert.end();
});
(0, _tapeCatch.default)('FACTORY KEY PARSER / should return undefined if a string is passed as a param and return undefined is set', function (assert) {
  var key = 'simple key';
  var keyParsed = (0, _factory.bucketing)(key);
  assert.equal(keyParsed, undefined, 'key parsed should return undefined');
  assert.end();
});
(0, _tapeCatch.default)('FACTORY KEY PARSER / should return undefined if a string is passed as a param and return undefined is set', function (assert) {
  var key = {
    bucketingKey: 'test_buck_key'
  };
  var keyParsed = (0, _factory.bucketing)(key);
  assert.equal(keyParsed, 'test_buck_key', 'key parsed should return undefined');
  assert.end();
});