"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs3/helpers/interopRequireDefault");

var _promise = _interopRequireDefault(require("@babel/runtime-corejs3/core-js-stable/promise"));

var _typeof2 = _interopRequireDefault(require("@babel/runtime-corejs3/helpers/typeof"));

var _setTimeout2 = _interopRequireDefault(require("@babel/runtime-corejs3/core-js-stable/set-timeout"));

var _tape = _interopRequireDefault(require("tape"));

var _timer = _interopRequireDefault(require("../../timeTracker/timer"));

var _timeTracker = _interopRequireDefault(require("../../timeTracker"));

/**
Copyright 2016 Split Software

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
**/
(0, _tape.default)('TIMER / should count the time between two tasks', function (assert) {
  var timerDuration = Math.floor(Math.random() * 1000); // In millis

  var stopTimer = (0, _timer.default)();
  (0, _setTimeout2.default)(function () {
    var elapsedTime = stopTimer();
    assert.true(elapsedTime >= timerDuration - 20, 'Timer should return correct difference (calculations made with +-20ms)');
    assert.true(elapsedTime <= timerDuration + 20, 'Timer should return correct difference (calculations made with +-20ms)');
    assert.end();
  }, timerDuration);
});
(0, _tape.default)('TIME TRACKER / should have the correct API', function (assert) {
  assert.equal((0, _typeof2.default)(_timeTracker.default.start), 'function', 'It should have the correct API.');
  assert.equal((0, _typeof2.default)(_timeTracker.default.stop), 'function', 'It should have the correct API.');
  assert.equal((0, _typeof2.default)(_timeTracker.default.TaskNames), 'object', 'It should have the correct API.');
  assert.end();
});
(0, _tape.default)('TIME TRACKER start() / should return the correct type', function (assert) {
  var promise = new _promise.default(function (res) {
    (0, _setTimeout2.default)(res, 1000);
  });
  promise.crap = true;

  var startNormal = _timeTracker.default.start(_timeTracker.default.TaskNames.SDK_READY);

  var startNormalFake = _timeTracker.default.start('fakeTask3');

  var startWithPromise = _timeTracker.default.start('fakeTask4', false, promise);

  assert.equal((0, _typeof2.default)(startNormal), 'function', 'If we call start without a promise, it will return the stop function,');
  assert.equal((0, _typeof2.default)(startNormal.setCollectorForTask), 'function', 'that has a function as well for setting the collector at a defered time, because it has a registered cb but no collector received.');
  assert.equal((0, _typeof2.default)(startNormalFake.setCollectorForTask), 'undefined', 'If no callback is registered for the task, no collectors setup function is attached to returned one.');
  assert.equal((0, _typeof2.default)(startWithPromise.then), 'function', 'But if we pass a promise, we will get a promise back, with the necessary callbacks already handled.');
  assert.end();
});
(0, _tape.default)('TIME TRACKER stop() / should stop the timer and return the time, if any', function (assert) {
  _timeTracker.default.start('test_task');

  var stopFromStart = _timeTracker.default.start('fakeTask5');

  var stopNotExistentTask = _timeTracker.default.stop('not_existent');

  var stopNotExistentTaskAndModifier = _timeTracker.default.stop('test_task', 'mod');

  assert.equal((0, _typeof2.default)(stopNotExistentTask), 'undefined', 'If we try to stop a timer that does not exist, we get undefined.');
  assert.equal((0, _typeof2.default)(stopNotExistentTaskAndModifier), 'undefined', 'If we try to stop a timer that does not exist, we get undefined.');
  assert.equal((0, _typeof2.default)(stopFromStart()), 'number', 'But if we stop an existing task from the startUnique() returned function, we get a number.');
  assert.end();
});