"use strict";

var _interopRequireDefault = require("@babel/runtime-corejs3/helpers/interopRequireDefault");

var _tapeCatch = _interopRequireDefault(require("tape-catch"));

var _os = _interopRequireDefault(require("os"));

var _ip = _interopRequireDefault(require("ip"));

var _settings = _interopRequireDefault(require("../../settings"));

var _constants = require("../../constants");

var _constants2 = require("../../../utils/constants");

/**
Copyright 2016 Split Software

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
**/
var IP_VALUE = _ip.default.address();

var HOSTNAME_VALUE = _os.default.hostname();

(0, _tapeCatch.default)('SETTINGS / Redis options should be properly parsed', function (assert) {
  var settingsWithUrl = (0, _settings.default)({
    core: {
      authorizationKey: 'dummy token'
    },
    storage: {
      type: 'REDIS',
      options: {
        url: 'test_url',
        host: 'h',
        port: 'p',
        db: 'bla',
        pass: 'nope',
        randomProp: 'I will not be present',
        connectionTimeout: 11,
        operationTimeout: 22
      },
      prefix: 'test_prefix'
    }
  });
  var settingsWithoutUrl = (0, _settings.default)({
    core: {
      authorizationKey: 'dummy token'
    },
    storage: {
      type: 'REDIS',
      options: {
        host: 'host',
        port: 'port',
        pass: 'pass',
        db: 'db',
        randomProp: 'I will not be present',
        connectionTimeout: 33,
        operationTimeout: 44
      },
      prefix: 'test_prefix'
    }
  });
  assert.deepEqual(settingsWithUrl.storage, {
    type: 'REDIS',
    prefix: 'test_prefix.SPLITIO',
    options: {
      url: 'test_url',
      connectionTimeout: 11,
      operationTimeout: 22
    }
  }, 'Redis storage settings and options should be passed correctly, url settings takes precedence when we are pointing to Redis.');
  assert.deepEqual(settingsWithoutUrl.storage, {
    type: 'REDIS',
    prefix: 'test_prefix.SPLITIO',
    options: {
      host: 'host',
      port: 'port',
      pass: 'pass',
      db: 'db',
      connectionTimeout: 33,
      operationTimeout: 44
    }
  }, 'Redis storage settings and options should be passed correctly, url settings takes precedence when we are pointing to Redis.');
  assert.end();
});
(0, _tapeCatch.default)('SETTINGS / IPAddressesEnabled should be overwritable and true by default', function (assert) {
  var settingsWithIPAddressDisabled = (0, _settings.default)({
    core: {
      authorizationKey: 'dummy token',
      IPAddressesEnabled: false
    }
  });
  var settingsWithIPAddressEnabled = (0, _settings.default)({
    core: {
      authorizationKey: 'dummy token'
    }
  });
  var settingsWithIPAddressDisabledAndConsumerMode = (0, _settings.default)({
    core: {
      authorizationKey: 'dummy token',
      IPAddressesEnabled: false
    },
    mode: _constants2.CONSUMER_MODE
  });
  var settingsWithIPAddressEnabledAndConsumerMode = (0, _settings.default)({
    core: {
      authorizationKey: 'dummy token'
    },
    mode: _constants2.CONSUMER_MODE
  });
  assert.equal(settingsWithIPAddressDisabled.core.IPAddressesEnabled, false, 'When creating a setting instance, it will have the provided value for IPAddressesEnabled');
  assert.equal(settingsWithIPAddressEnabled.core.IPAddressesEnabled, true, 'and if no IPAddressesEnabled was provided, it will be true.');
  assert.deepEqual({
    ip: false,
    hostname: false
  }, settingsWithIPAddressDisabled.runtime, 'When IP address is disabled in standalone mode, the runtime setting properties (ip and hostname) have to be false, to avoid be added as request headers.');
  assert.deepEqual({
    ip: _constants.NA,
    hostname: _constants.NA
  }, settingsWithIPAddressDisabledAndConsumerMode.runtime, 'When IP address is disabled in consumer mode, the runtime setting properties (ip and hostname) will have a value of "NA".');
  assert.deepEqual({
    ip: IP_VALUE,
    hostname: HOSTNAME_VALUE
  }, settingsWithIPAddressEnabled.runtime, 'When IP address is enabled, the runtime setting will have the current ip and hostname values.');
  assert.deepEqual({
    ip: IP_VALUE,
    hostname: HOSTNAME_VALUE
  }, settingsWithIPAddressEnabledAndConsumerMode.runtime, 'When IP address is enabled, the runtime setting will have the current ip and hostname values.');
  assert.end();
});