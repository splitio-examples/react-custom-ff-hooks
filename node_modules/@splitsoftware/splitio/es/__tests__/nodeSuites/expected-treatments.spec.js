import _regeneratorRuntime from "@babel/runtime-corejs3/regenerator";
import _concatInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/concat";
import _asyncToGenerator from "@babel/runtime-corejs3/helpers/esm/asyncToGenerator";
import { SplitFactory } from '../../';
import fs from 'fs';
import rl from 'readline';
import splitChangesMockReal from '../mocks/splitchanges.real.json';
export default function (_x, _x2, _x3, _x4) {
  return _ref.apply(this, arguments);
}

function _ref() {
  _ref = _asyncToGenerator(
  /*#__PURE__*/
  _regeneratorRuntime.mark(function _callee(config, settings, mock, assert) {
    var splitio, client, parser;
    return _regeneratorRuntime.wrap(function _callee$(_context2) {
      while (1) {
        switch (_context2.prev = _context2.next) {
          case 0:
            mock.onGet(settings.url('/splitChanges?since=-1')).reply(200, splitChangesMockReal);
            splitio = SplitFactory(config);
            client = splitio.client();
            _context2.next = 5;
            return client.ready();

          case 5:
            parser = rl.createInterface({
              terminal: false,
              input: fs.createReadStream(require.resolve('../mocks/expected-treatments.csv'))
            });
            parser.on('line', function (line) {
              var parts = line.toString('utf8').split(',');

              if (parts.length === 2) {
                var _context;

                var key = parts[0];
                var treatment = parts[1];
                assert.equal(client.getTreatment(key, 'real_split'), treatment, _concatInstanceProperty(_context = "Checking expected treatment \"".concat(treatment, "\" for key: ")).call(_context, key));
              }
            }).on('close', function () {
              return client.destroy().then(assert.end);
            });

          case 7:
          case "end":
            return _context2.stop();
        }
      }
    }, _callee);
  }));
  return _ref.apply(this, arguments);
}