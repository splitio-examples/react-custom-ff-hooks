import ClientWithInputValidationLayer from './inputValidation';
export default function NodeClientFactory(context) {
  var client = ClientWithInputValidationLayer(context);
  client.isBrowserClient = false;
  return client;
}