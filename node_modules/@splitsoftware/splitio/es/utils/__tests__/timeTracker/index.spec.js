import _Promise from "@babel/runtime-corejs3/core-js-stable/promise";
import _typeof from "@babel/runtime-corejs3/helpers/esm/typeof";
import _setTimeout from "@babel/runtime-corejs3/core-js-stable/set-timeout";

/**
Copyright 2016 Split Software

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
**/
import tape from 'tape';
import timer from '../../timeTracker/timer';
import tracker from '../../timeTracker';
tape('TIMER / should count the time between two tasks', function (assert) {
  var timerDuration = Math.floor(Math.random() * 1000); // In millis

  var stopTimer = timer();

  _setTimeout(function () {
    var elapsedTime = stopTimer();
    assert["true"](elapsedTime >= timerDuration - 20, 'Timer should return correct difference (calculations made with +-20ms)');
    assert["true"](elapsedTime <= timerDuration + 20, 'Timer should return correct difference (calculations made with +-20ms)');
    assert.end();
  }, timerDuration);
});
tape('TIME TRACKER / should have the correct API', function (assert) {
  assert.equal(_typeof(tracker.start), 'function', 'It should have the correct API.');
  assert.equal(_typeof(tracker.stop), 'function', 'It should have the correct API.');
  assert.equal(_typeof(tracker.TaskNames), 'object', 'It should have the correct API.');
  assert.end();
});
tape('TIME TRACKER start() / should return the correct type', function (assert) {
  var promise = new _Promise(function (res) {
    _setTimeout(res, 1000);
  });
  promise.crap = true;
  var startNormal = tracker.start(tracker.TaskNames.SDK_READY);
  var startNormalFake = tracker.start('fakeTask3');
  var startWithPromise = tracker.start('fakeTask4', false, promise);
  assert.equal(_typeof(startNormal), 'function', 'If we call start without a promise, it will return the stop function,');
  assert.equal(_typeof(startNormal.setCollectorForTask), 'function', 'that has a function as well for setting the collector at a defered time, because it has a registered cb but no collector received.');
  assert.equal(_typeof(startNormalFake.setCollectorForTask), 'undefined', 'If no callback is registered for the task, no collectors setup function is attached to returned one.');
  assert.equal(_typeof(startWithPromise.then), 'function', 'But if we pass a promise, we will get a promise back, with the necessary callbacks already handled.');
  assert.end();
});
tape('TIME TRACKER stop() / should stop the timer and return the time, if any', function (assert) {
  tracker.start('test_task');
  var stopFromStart = tracker.start('fakeTask5');
  var stopNotExistentTask = tracker.stop('not_existent');
  var stopNotExistentTaskAndModifier = tracker.stop('test_task', 'mod');
  assert.equal(_typeof(stopNotExistentTask), 'undefined', 'If we try to stop a timer that does not exist, we get undefined.');
  assert.equal(_typeof(stopNotExistentTaskAndModifier), 'undefined', 'If we try to stop a timer that does not exist, we get undefined.');
  assert.equal(_typeof(stopFromStart()), 'number', 'But if we stop an existing task from the startUnique() returned function, we get a number.');
  assert.end();
});