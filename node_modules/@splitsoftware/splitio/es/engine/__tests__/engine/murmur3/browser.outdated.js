import _parseInt from "@babel/runtime-corejs3/core-js-stable/parse-int";
import _slicedToArray from "@babel/runtime-corejs3/helpers/esm/slicedToArray";
import _setTimeout from "@babel/runtime-corejs3/core-js-stable/set-timeout";
import _getIterator from "@babel/runtime-corejs3/core-js/get-iterator";
import _trimInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/trim";

/**
Copyright 2016 Split Software

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
**/
import axios from 'axios';
import tape from 'tape-catch';
import utils from '../../../engine/murmur3';
tape('MURMUR3', function (t) {
  function assertText(text, assert) {
    var lines = _trimInstanceProperty(text).call(text).split(/\r\n|\n|\r/);

    assert.plan(2 * lines.length);
    var _iteratorNormalCompletion = true;
    var _didIteratorError = false;
    var _iteratorError = undefined;

    try {
      var _loop = function _loop() {
        var parts = _step.value;

        _setTimeout(function () {
          var _parts$split = parts.split(','),
              _parts$split2 = _slicedToArray(_parts$split, 4),
              seed = _parts$split2[0],
              key = _parts$split2[1],
              hash = _parts$split2[2],
              bucket = _parts$split2[3];

          seed = _parseInt(seed, 10);
          hash = _parseInt(hash, 10);
          bucket = _parseInt(bucket, 10);
          assert.equal(utils.hash(key, seed), hash);
          assert.equal(utils.bucket(key, seed), bucket);
        }, Math.random() * 10);
      };

      for (var _iterator = _getIterator(lines), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
        _loop();
      }
    } catch (err) {
      _didIteratorError = true;
      _iteratorError = err;
    } finally {
      try {
        if (!_iteratorNormalCompletion && _iterator["return"] != null) {
          _iterator["return"]();
        }
      } finally {
        if (_didIteratorError) {
          throw _iteratorError;
        }
      }
    }
  }

  t.test('validate hashing behavior using basic dataset', function (assert) {
    axios.get('/base/engine/__tests__/engine/mocks/murmur3-sample-data-v2.csv').then(function (response) {
      return response.data;
    }).then(function (text) {
      return assertText(text, assert);
    })["catch"](function (error) {
      return assert.error(error);
    });
  });
  t.test('validate hashing behavior using chinese dataset', function (assert) {
    axios.get('/base/engine/__tests__/engine/mocks/murmur3-sample-data-non-alpha-numeric-v2.csv').then(function (response) {
      return response.data;
    }).then(function (text) {
      return assertText(text, assert);
    })["catch"](function (error) {
      return assert.error(error);
    });
  });
});